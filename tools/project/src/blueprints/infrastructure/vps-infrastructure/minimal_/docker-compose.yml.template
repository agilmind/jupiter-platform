version: '3.8'

# Docker Compose mínimo (solo proxy y utilidades básicas)
services:
  # Nginx - Proxy inverso
  nginx:
    image: nginx:alpine
    container_name: <%=projectName%>-nginx
    # Configuración mínima
    # ...

# Networks pendientes
