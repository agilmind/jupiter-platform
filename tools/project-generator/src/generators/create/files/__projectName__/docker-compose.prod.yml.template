version: "3.9"
services:
  <%= projectName %>-app:
    image: your-registry/<%= projectName %>-app:latest # Replace with your production image
    ports:
      - "80:3000" # Adjust ports as needed
    environment:
      NODE_ENV: production
    restart: always
