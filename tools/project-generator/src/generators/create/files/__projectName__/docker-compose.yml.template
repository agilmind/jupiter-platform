version: "3.9"
services:
  <%= projectName %>-app:
    image: node:18-alpine
    ports:
      - "3000:3000" # Adjust ports as needed
    volumes:
      - ./:/app
    working_dir: /app
    command: npm run start:dev # Or your development start command
    environment:
      NODE_ENV: development
