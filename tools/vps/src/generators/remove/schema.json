{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "id": "create",
  "title": "Remove VPS Project",
  "type": "object",
  "properties": {
    "projectName": {
      "type": "string",
      "description": "Nombre del proyecto VPS",
      "pattern": "^[a-zA-Z][a-zA-Z0-9-]*$",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["projectName"]
}
