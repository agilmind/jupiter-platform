name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  main:
    name: Nx Cloud - Main Job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # Configuración de Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'
          cache: 'npm'

      # Instalación de dependencias con caché
      - name: Cache node modules
        id: cache-nodemodules
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        if: steps.cache-nodemodules.outputs.cache-hit != 'true'
        run: npm ci

      # Verificación del formato de código (visible y temprana)
      - name: Check format
        id: format
        run: |
          echo "::group::🔍 Verificando formato del código"
          npx nx format:check
          echo "::endgroup::"
        continue-on-error: true

      # Análisis estático con información detallada
      - name: Lint
        id: lint
        run: |
          echo "::group::🔍 Ejecutando análisis de código"
          npm run ci:lint
          echo "::endgroup::"

      # Tests
      - name: Test
        id: test
        run: |
          echo "::group::🧪 Ejecutando tests"
          npm run ci:test
          echo "::endgroup::"

      # Build
      - name: Build
        id: build
        run: |
          echo "::group::🔨 Compilando aplicaciones"
          npm run ci:build
          echo "::endgroup::"

      # Resumen final
      - name: CI Summary
        if: always()
        run: |
          echo "### Resumen de CI 📊" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Verificación | Estado |" >> $GITHUB_STEP_SUMMARY
          echo "| ------------ | ------ |" >> $GITHUB_STEP_SUMMARY
          echo "| Formato | ${{ steps.format.outcome == 'success' && '✅ Pasó' || '⚠️ No cumple con estándares' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Lint | ${{ steps.lint.outcome == 'success' && '✅ Pasó' || '❌ Falló' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Test | ${{ steps.test.outcome == 'success' && '✅ Pasó' || '❌ Falló' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Build | ${{ steps.build.outcome == 'success' && '✅ Pasó' || '❌ Falló' }} |" >> $GITHUB_STEP_SUMMARY

      # Solo falla el CI si los pasos críticos fallan (formato no es crítico)
      - name: Check Status
        if: steps.lint.outcome != 'success' || steps.test.outcome != 'success' || steps.build.outcome != 'success'
        run: exit 1
