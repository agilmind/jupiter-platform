version: '3.8'

# Docker Compose para entornos múltiples (staging + prod) en un solo VPS
services:
  # Nginx - Proxy inverso compartido
  nginx:
    image: nginx:alpine
    container_name: jupiter-nginx
    # Configuración pendiente para múltiples entornos
    # ...

  # Servicios para producción
  # ...

  # Servicios para staging
  # ...
# Networks y Volumes pendientes
